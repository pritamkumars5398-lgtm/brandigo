export const metadata = {
  title: "Admin Portal — Brandingo",
  description: "Management console for leads and payments",
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
